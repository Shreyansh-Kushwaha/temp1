fastapi==0.115.6
uvicorn[standard]==0.32.1
asyncpg==0.30.0
openai==2.36.0
python-dotenv==1.0.1
httpx==0.28.1
motor==3.6.0
gtts==2.5.4
supabase==2.30.0
# Server-side PDF rendering. Safe on Python 3.12 (greenlet has prebuilt
# cp312 wheels — no source compile). pdf_service.py lazy-imports it so the
# backend boots fine even if the chromium browser binary isn't available.
playwright==1.49.1
aiosmtplib==3.0.2
